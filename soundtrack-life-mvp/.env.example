# 可选：接入任意兼容 OpenAI Chat Completions 的对话模型。
# 不配置时应用自动运行在 mock 模式，演示链路仍可完整跑通。
VITE_BACKEND_BASE_URL=http://localhost:8787
VITE_ENABLE_TUNING_DEMO=false

# BFF server-only settings. Never expose these as VITE_* values.
ALLOWED_ORIGINS=http://localhost:5173,https://ilovetochangetheworld.github.io
ANALYSIS_BASE_URL=http://127.0.0.1:8790
ENABLE_TUNING_DEMO=false
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini

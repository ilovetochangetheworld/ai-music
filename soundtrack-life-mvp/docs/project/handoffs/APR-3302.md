# APR-3302 任务交接

- 目标：增加首次设备延迟校准、本地复用和重新校准，并让可信结果进入实时对齐与唱后节奏分析。
- 负责人 / 智能体：Codex
- Base SHA / 分支：`45f5a93` / `codex/apr-3302-latency-calibration`
- 依赖：APR-3101 实时视觉辅助基础。
- 允许修改的模块：共享契约与 Schema、练歌准备/校准/演唱页面、延迟估计与本地存储、评分输入、对应样式和文档。
- 验收标准：首次开唱前可校准或跳过；至少 4 个有效样本才可能生效；有效结果本地保存 30 天并复用；准备页可重新校准；低置信度不强行修正；麦克风拒绝可返回或跳过。
- 需同步的文档：PRD、技术架构、ROADMAP、本交接。
- 已运行命令：`git diff --check`、`npm run typecheck`、`npm run lint`、`npm run test`、`npm run test:analysis`、`npm run catalog:validate`、`npm run build`。
- 验证证据：8 个 Vitest 文件共 19 项通过；Python 2 项通过；曲库 3 项有效且 0 警告；生产构建通过。新增测试覆盖稳定样本、中位数异常剔除、样本不足、过期失效和评分时间轴补偿。
- 风险 / 未完成项：浏览器跟拍只能估计综合链路偏移，不能分离纯硬件延迟与用户动作响应；蓝牙路由切换仍需用户主动重测。当前浏览器控制连接不可用，390×844 视觉与真实麦克风权限/跟拍仍需真机验收。
- Head SHA / PR：当前分支 HEAD / 待创建 PR。

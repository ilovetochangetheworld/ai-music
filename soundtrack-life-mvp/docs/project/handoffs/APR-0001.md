# APR-0001 handoff — AI 练歌房 foundation

## Scope completed on branch

- Added the root agent contract, product/architecture sources of truth, roadmap, templates, issue/PR templates, and decision log.
- Replaced the active routes with the mobile practice loop and moved legacy experiments to `/lab`.
- Removed QQ login, playlist code, QQ API proxying, and QQ systemd units.
- Added shared TypeScript/JSON contracts, a three-entry catalog, one playable prototype package, deterministic local quality gates, report storage, and IndexedDB growth summaries.
- Added the Node practice BFF and CPU FastAPI skeleton with 25MB/24-hour controls, async status/report flow, no-score fallback, and disabled tuning endpoint.
- Added Vitest, Python tests, catalog validation, deployment units, and documentation.

## Verification

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run test:analysis`
- `npm run catalog:validate`
- `npm run build`
- Local API smoke: `POST 202 -> GET completed -> report insufficient_data`; pitch and total remain null without reviewed notes.
- Mobile visual check at 390×844: home, catalog, setup, and pre-microphone sing stage.

## Risks and unfinished work

- `trajectory/notes.json` and `phrases.json` are explicit placeholders; APR-1001/1003 must produce and manually review references before pitch/rhythm scoring can ship.
- Two catalog entries are intentionally `preparing`; the three-song milestone is not complete.
- The web client does not upload recordings because the explicit server-upload consent UI is not designed yet. Local recording remains functional.
- GPU tuning is only represented by a disabled contract/feature flag; APR-4001 research has not started.
- Golden-set correlation, Playwright automation, real-device regression, production reverse proxy, and GitHub Pages deployment remain open.

## Integration metadata

Base/head SHA must be filled by the integration owner when the PR is created. `STATUS.md` was intentionally not changed because this work is not merged to `main`.

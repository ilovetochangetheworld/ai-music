# APR-0001 交接——AI 练歌房基础

## 分支内已完成范围

- 新增根目录智能体规范、产品/架构事实源、路线图、模板、Issue/PR 模板和决策记录。
- 主路由改为移动端练歌闭环，旧实验迁入 `/lab`。
- 删除 QQ 登录、歌单代码、QQ API 代理和 QQ systemd 服务。
- 新增共享 TypeScript/JSON 契约、三条歌曲目录、一个可播放原型包、确定性本地门禁、报告存储和 IndexedDB 成长摘要。
- 新增 Node 练歌 BFF 和 CPU FastAPI 骨架，包含 25MB/24 小时限制、异步状态/报告流程、无分数降级和禁用的修音接口。
- 新增 Vitest、Python 测试、曲库校验、部署单元和文档。

## 验证

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run test:analysis`
- `npm run catalog:validate`
- `npm run build`
- 本地 API 冒烟：`POST 202 → GET completed → report insufficient_data`；没有已审核音符时音高和总分保持 null。
- 390×844 移动端视觉检查：首页、曲库、设置和麦克风启动前演唱页。

## 风险与未完成项

- `trajectory/notes.json` 和 `phrases.json` 当时为明确占位；APR-1001/1003 必须产出并人工审核参考数据后才能发布音高/节奏评分。
- 两条曲库记录当时有意标记为 `preparing`，三首歌曲里程碑尚未完成。
- Web 客户端未上传录音，因为尚未设计明确的服务端上传同意界面；本地录音仍可用。
- GPU 修音只有禁用的契约/功能开关；APR-4001 研究尚未开始。
- Golden set 相关性、Playwright 自动化、真机回归、生产反向代理和 GitHub Pages 发布仍待完成。

## 集成信息

创建 PR 时由集成负责人填写 base/head SHA。由于工作尚未合并到 `main`，当时有意不修改 `STATUS.md`。
